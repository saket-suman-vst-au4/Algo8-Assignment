module.exports = {
  mongourl: process.env.MOGOURL, //here in production ,we will be using Environment varaible which will be fetched from heroku here in this case if i deploy it in heroku
}; //Basically we hide these details for security purpose.
